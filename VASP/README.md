This folder has the code required to run Ab initio Molecular Dynamics within the Vienna Ab initio Simulation Package (VASP)
This includes both optimzing the data as well as running the AIMD, further usuage of VASP is found within implementing the Neural Networks in N2P2
Also Includes code that allows us to create graphs which are considerably easier to understnad than hundreds and hundreds lines of codes.
